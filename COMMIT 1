#include <iostream>
using namespace std;

int main() {
    int firstTemp, secondTemp, finalTemp;

    // Get the first temperature reading
    cout << "Enter the first temperature reading: ";
    cin >> firstTemp;
